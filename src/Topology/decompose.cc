
#include "decompose.hh"
#include "iterator.hh"

namespace Topo
{
#if 0
std::vector<Wrap<Type::BODY>> decompose(Wrap<Type::BODY> _body)
{
  Iterator<Type::BODY, Type::EDGE> edges(_body);
  for (auto ed : edges)
  {
    Iterator<Type::EDGE, Type::VERTEX> ed_it(ed);

  }
}
#endif

} // namespace Topo
